services:
  bike-rental-app:
    build: .
    container_name: c270-bike-rental-wallet
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./backend/data:/app/backend/data
    restart: unless-stopped
